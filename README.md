# NYC Taxi Data Processing (Go)

## Notes:

https://docs.gitlab.com/runner/install/linux-repository.html

Run a specific set of tests:

```bash
time docker run -v $(pwd):/opt/project -it taxi_data_go go test -v -test.run RatecodeID
```

test
